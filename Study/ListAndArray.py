import numpy as np 
import array as arr 
from array import *
a= [1,2,3]
my_array = np.array(a)
print(my_array)

#
#Array
#Kích thước cố định.
#Có thể lưu trữ dữ liệu kiểu nguyên thủy và đối tượng.
#Tốc độ lưu trữ và thao tác nhanh hơn.	
#Chỉ có thuộc tính length.	

#ArrayList
#Kích thước có thể thay đổi được.
#Chỉ có thể lưu trữ dữ liệu kiểu đối tượng. Kể từ Java 5,
# kiểu nguyên thủy được tự động chuyển đổi trong các đối tượng được gọi là auto-boxing.
#Tốc độ lưu trữ vào thao tác chậm hơn.
#Có nhiều phương thức để thao tác với dữ liệu.